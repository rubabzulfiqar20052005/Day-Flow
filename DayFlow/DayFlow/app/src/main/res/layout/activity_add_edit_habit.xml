<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="18dp"
    android:background="#FFFFFF">

    <TextView
        android:id="@+id/tvHabitHeader"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Add Habit"
        android:textStyle="bold"
        android:textSize="20sp"
        android:textColor="#111827"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <EditText
        android:id="@+id/etHabitTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Habit name (e.g., Drink Water)"
        android:textColor="#111827"
        android:textColorHint="#9CA3AF"
        android:background="@drawable/bg_rounded_input_light"
        android:layout_marginTop="14dp"
        app:layout_constraintTop_toBottomOf="@id/tvHabitHeader"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <Button
        android:id="@+id/btnSaveHabit"
        android:layout_width="0dp"
        android:layout_height="54dp"
        android:text="Save Habit"
        android:textAllCaps="false"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:background="@drawable/bg_primary_btn"
        android:layout_marginTop="14dp"
        app:layout_constraintTop_toBottomOf="@id/etHabitTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
